KOPYKAT — IDE HANDOFF / PROGRESS UPDATE
Last Updated: August 22, 2026
Updated By: Antigravity (Claude Sonnet 4.6 Thinking)
Previous Update By: GPT-5.6 Luna

PRODUCT MOTTO: "It should just work."
This is the standard every feature is held to. If a customer has to think,
read a tutorial, or contact support to use a feature — it is not finished.

PURPOSE
Bring the IDE-side AI fully up to speed without repeating completed work.
This file is the single source of truth for project state and next steps.

============================================================
MANDATORY ENGINEERING PROTOCOL — ALL AI SESSIONS
============================================================
This protocol applies to every AI working on this project regardless of model.
It is not optional. It is not a suggestion.

CORE PRINCIPLE
Never confuse:
- knowing a best practice
- mentioning a best practice
- implementing a best practice
- proving that the implementation actually satisfies it

Default posture: "Assume there is a defect until the implementation provides
evidence otherwise."

IMPLEMENTATION MODE — follow in order for every feature:
1. Understand the complete execution path.
2. Identify affected components.
3. Identify existing contracts and invariants.
4. Identify dependencies between files/services.
5. Identify security and economic consequences.
6. Implement the smallest correct change.
7. Inspect surrounding code for compatibility.
8. Verify all callers and consumers of changed interfaces.
9. Generate or update tests.
10. Perform adversarial review AFTER implementation.

Do not stop when the code "looks right."

GLOBAL INVARIANT CHECKING
Before declaring a feature complete, explicitly verify its invariants:
- A value written by one component must be interpreted identically by every consumer.
- A successful payment must produce exactly one entitlement.
- A failed payment must not produce an entitlement.
- A successful generation must consume the correct amount of usage.
- A failed generation must not permanently consume usage.
- Concurrent requests must not bypass limits.
- Authorization must be enforced consistently across equivalent endpoints.
- Secrets must never be exposed through logs, errors, responses, or source.
- Frontend claims must correspond to actual backend behavior.
- Database state must remain consistent after partial failure.

CROSS-FILE REASONING
Never review a changed function in isolation.
When changing any API field, model, enum, billing identifier, auth mechanism,
response, or config variable — find every producer AND consumer.
Check: PRODUCER -> STORAGE -> TRANSPORT -> CONSUMER
Check: FRONTEND -> API -> SERVICE -> DATABASE -> EXTERNAL PROVIDER

SECURITY REVIEW — assume an attacker controls everything:
request params, headers, cookies, API keys, JWTs, uploaded files,
webhook payloads, IDs, timing, concurrency, repeated requests.
For any vulnerability found, identify: attack / code path / consequence / fix / test.

FINANCIAL REVIEW — assume the customer will:
click twice, retry, send concurrent requests, receive duplicate webhooks,
cancel immediately, dispute, manipulate client-side values.
Verify: payment -> webhook -> entitlement -> usage -> expiration -> cancellation -> refund.
Never trust the frontend for price, entitlement, balance, or authorization.

FAILURE-FIRST ENGINEERING
Ask not only "what happens when everything works?" but also
"what happens when every component fails?"
A graceful failure is preferable to silent corruption.

SECRETS
Never silently invent security-sensitive defaults.
If a required secret is missing: fail startup with an explicit error.
Never expose keys, tokens, or credentials in logs, exceptions, responses, or source.

ADVERSARIAL REVIEW — after implementing, switch roles:
A) SECURITY AUDITOR: "How can I break this?"
B) FINANCIAL AUDITOR: "How can I obtain value without paying?"
C) MALICIOUS USER: "How can I abuse the intended workflow?"
D) QA ENGINEER: "What edge case did the developer forget?"
E) SYSTEM ARCHITECT: "What happens when this scales?"
F) MAINTAINER: "What happens when someone changes this in six months?"
G) CUSTOMER: "What does the product promise that the backend cannot deliver?"

NO FALSE CONFIDENCE
Never say "This is secure" / "This is production-ready" / "Everything looks good"
without evidence. Use precise language:
"Verified:" / "Not verified:" / "Potential vulnerability:" / "Requires testing:"

PRIORITY CLASSIFICATION
P0 — catastrophic: security breach, payment corruption, data loss, auth bypass
P1 — serious: reliability issue, exploitable business logic, incorrect billing
P2 — engineering defect: maintainability, performance, incomplete edge cases
P3 — improvement: optimization, UX, refactoring, documentation
Never bury a P0/P1 beneath cosmetic recommendations.

FINAL REVIEW FORMAT (required before declaring any task complete)
IMPLEMENTED: what changed.
VERIFIED: what was actually verified.
NOT VERIFIED: what could not be proven.
INVARIANTS: important system rules identified.
SECURITY: vulnerabilities found or ruled out.
BILLING/ECONOMICS: financial risks found or ruled out.
RELIABILITY: failure and concurrency risks.
CONTRACT CONSISTENCY: frontend/backend/API/database consistency.
TESTS: tests created or executed.
REMAINING RISKS: anything still requiring attention.
FINAL VERDICT: READY / READY WITH WARNINGS / NOT READY / BLOCKED

MOST IMPORTANT RULE
Do not optimize for agreeing with the user.
Do not defend previous code merely because you wrote it.
If the requirement is flawed, say so.
If the architecture is wrong, say so.
The goal is not "make the code work."
The goal is "make the system difficult to break."

============================================================
LIVING INVARIANT LEDGER
============================================================
Every time billing, authentication, integrations, credits, or permissions
are touched, update this ledger and verify the entire codebase against it.

BILLING INVARIANTS
- Plan keys in billing.py PLANS dict must exactly match frontend checkout button values.
  Current valid plan keys: boutique, standard, megastore
- Campaign deduction uses atomic SQL: UPDATE users SET generations = generations - 1
  WHERE id = :uid AND generations >= 1. Never use ORM read-modify-write.
- Stripe webhook idempotency enforced via StripeEvent.event_id unique constraint.
- Entitlement is granted only after Stripe confirms payment (webhook, not frontend).

AUTHENTICATION INVARIANTS
- All protected endpoints use Depends(get_current_user_apikey).
- JWT secret must be set via SECRET_KEY env var. Never fall back to a generated default.
- Credentials encrypted with Fernet key from ENCRYPTION_KEY env var.
- Founder account (thepros2014@gmail.com) has no special startup bypass — use DB directly.

GENERATION INVARIANTS
- A failed generation must not permanently consume a credit.
- A successful generation must consume exactly 1 credit.
- Concurrent generation requests cannot bypass the credit check (atomic SQL enforces this).

INTEGRATION INVARIANTS
- No push function may report success unless the destination API returns 2xx.
- All integration credentials are encrypted at rest (auth.encrypt_credentials).
- No credential value is ever logged or returned in an API response.
- SSRF protection: integrations may only call HTTPS external URLs.

FRONTEND/BACKEND CONTRACT
- PushRequest.platform Literal must include every platform shown in the UI.
  Current valid values: wordpress, mailchimp, hubspot, shopify, webflow,
                        amazon, ebay, walmart, temu
- APP_VERSION in both HTML files must be incremented on every frontend deploy
  to trigger client-side cache wipe. Current version: 1.0.8

============================================================
PROJECT VISION
============================================================
KopyKat is a full-stack e-commerce automation system for small business owners.
It is NOT just an "AI copywriting tool."

The intended product:
  "Give KopyKat your product catalog once.
   It will write, optimize, and distribute your marketing and inventory
   across every platform you sell on — automatically."

Target customer: Solo operators and small business owners running stores
on Shopify, Amazon, eBay, Walmart, Temu, and similar platforms who cannot
afford a full marketing team.

Pricing tiers:
  Boutique   — $97/mo   (250 campaigns)
  Standard   — $297/mo  (1,000 campaigns)
  Megastore  — $897/mo  (5,000 campaigns)

Deployed at: https://kopykat.onrender.com

============================================================
WORKING PRINCIPLES (DO NOT VIOLATE)
============================================================
1. Security first. Reliability first.
2. Surgical code changes — NEVER wholesale-replace a core file.
   Pattern: FETCH current file -> MAKE surgical change -> VERIFY -> COMMIT.
3. Never claim something is fixed or verified when it was not.
4. Small, reversible commits.
5. Never execute arbitrary AI-generated code.
6. Never expose customer credentials to the AI model.
7. Fail closed when authentication, validation, or platform behavior is ambiguous.
8. No emojis in code or HTML — the user has explicitly banned them.
9. Never blindly retry non-idempotent writes.
10. Never report "published/synchronized" unless destination confirms success.

============================================================
COMPLETED WORK — PREVIOUS SESSIONS (DO NOT REDO)
============================================================
- Stripe plan/fulfillment synchronization
- Billing plan-name synchronization (boutique/standard/megastore)
- Password-reset compatibility
- JWT/encryption secret handling
- Removal of stale scheduler references
- Background worker deployment (worker.py runs alongside web service)
- Production DATABASE_URL enforcement
- Integration SSRF protections
- Removal of fake marketplace "success" responses
- Founder entitlement handling (removed hard-coded startup override)
- Recovery of server/main.py after an accidental overly broad replacement
- CSV/dashboard XSS hardening
- Dashboard status updates moved away from unsafe innerHTML
- Atomic generation-credit deduction (conditional SQL UPDATE, not ORM read-modify-write)
- Stripe event ledger/idempotency protections
- Revenue uniqueness protections
- GitHub Actions CI workflow (.github/workflows/ci.yml, commit aeb1c04)
- Universal Connector Engine backend installed:
    server/connector_engine.py  — SSRF-safe OpenAPI discovery
    server/connector_registry.py — restricted validated connector runtime

============================================================
COMPLETED WORK — THIS SESSION (August 22, 2026)
============================================================
All commits pushed to main on GitHub. Local is fully synced.

REBRAND
- Full rebrand from SnapCopy AI to KopyKat across all files
- GitHub repo renamed to thepros2014/kopykat
- All URLs updated to kopykat.onrender.com

SECURITY FIXES
- P0: Atomic billing deduction to prevent omni-campaign generation bypass
- P1: ProxyHeadersMiddleware added to main.py for correct IP detection behind Render LB
- P1: Billing PLANS keys synchronized with frontend checkout button plan names
- Information disclosure: Raw exception strings replaced with generic messages
- Frontend 422 error parsing fixed (was showing [object Object])

UI / UX
- Rebuilt missing dashboard HTML sections:
    section-generator, section-omni, section-integrations
  These existed only as JS functions with no corresponding HTML — sidebar buttons
  were throwing null reference errors silently.
- Replaced developer-focused Quick Start with 3-step Store Owner Onboarding
- Replaced static "Try it live" demo widget with CSS-animated Omni-Campaign UI
- Built full Features section on landing page (nav link was broken — no target section existed)
- Rewrote hero copy to remove API/developer language
- Added force cache wipe mechanism (APP_VERSION string in localStorage)
  Current version: 1.0.8
  To force all users to clear cache on next deploy: increment this string in
  both frontend/index.html and frontend/dashboard.html

INTEGRATIONS
- Added backend push functions in server/integrations.py:
    push_to_amazon()   — Amazon SP-API (LWA OAuth token flow)
    push_to_ebay()     — eBay Inventory REST API
    push_to_walmart()  — Walmart Marketplace API
    push_to_temu()     — Temu Seller Center Open Platform API
- All four wired into background_push() router
- server/models.py PushRequest Literal updated to include all 7 platforms
- Dashboard UI: Connect cards added for Shopify, Amazon, eBay, Walmart, Temu
- saveIntegration() JS updated to handle all new platform credential fields
- Omni-Campaign push UI converted from single-select dropdowns to
  multi-select checkbox groups (Product / Blog / Email syndication hubs)
- runOmniPush() JS rewritten to fire parallel fetch() calls to all checked platforms

BULK CATALOG SYNC (CSV IMPORTER)
- New "Bulk Sync CSV" sidebar button and dashboard section added
- New backend endpoint: POST /api/catalog/parse-csv
    Accepts: multipart file upload
    Uses AI (GPT-4o or Gemini) to semantically map column headers regardless
    of what the customer named them (e.g. "item_title_v2" maps to Product Name)
    Returns: list of {name, desc} items + mapping_used for user confirmation
    Limit: 100 products per upload (MVP cap)
- New function in server/ai_engine.py: analyze_csv_mapping()
    Falls back to heuristic matching if no AI key is configured
- Frontend: parseCSV() sends file to backend, displays AI-mapped results
- Frontend: startBulkSyndication() loops through products, generates Omni-Campaign
  for each, then pushes to all checked platforms in parallel

ADMIN ACCOUNT
- thepros2014@gmail.com is the founder test account
- NOTE: The startup auto-upgrade was previously removed for security
  To grant megastore access, update the user row directly in the database:
    UPDATE users SET plan='megastore', generations=5000, monthly_limit=5000
    WHERE email='thepros2014@gmail.com';

============================================================
CURRENT TODO LIST — PRIORITIZED
============================================================

IMMEDIATE / HIGH PRIORITY

[ ] TODO-1: "Add Any Platform" UI (Phase 1 of Universal Connector Engine)
    The backend is fully installed. The customer-facing UI does not exist yet.
    Build a new "Add Platform" flow in the Integrations tab:
      - Input: URL of API documentation or OpenAPI spec
      - Call: POST /api/connector/discover (wire to connector_engine.py)
      - Display: discovered auth method, available operations, capabilities
      - DO NOT auto-activate — require explicit customer authorization step
    Reference: server/connector_engine.py and server/connector_registry.py
    DO NOT rebuild discovery or runtime — they already exist.

[ ] TODO-2: Connector Database Model (Phase 2)
    Add a CustomConnector table to database.py:
      - id, user_id, name, base_url, spec (JSON), status, created_at, updated_at
      - status values: draft / validated / active / disabled
      - Never store raw credentials in the spec column

[ ] TODO-3: Connector OAuth / Credential Subsystem (Phase 3)
    - OAuth authorization flow with callback state protection
    - Token storage (encrypted, same pattern as UserIntegration.credentials)
    - Token refresh and revocation
    - Display OAuth scopes to customer before authorization
    - Least-privilege scope requests

[ ] TODO-4: Sandbox / Connection Test (Phase 4)
    - Authenticated read-only test against the connector before activation
    - Response and schema validation
    - No writes during test
    - Explicit customer activation step after successful test

[ ] TODO-5: Image-to-Copy (Vision AI) — Step 3 of 3
    Add image upload to the Omni-Campaign Wizard.
    Customer uploads a product photo.
    AI (GPT-4o Vision or Gemini Pro Vision) reads the image and writes the campaign.
    Backend: accept multipart image in /api/campaign/generate, pass base64 to AI.
    Frontend: add file input to omni-campaign wizard above the keyword field.

[ ] TODO-6: Email Verification Endpoints
    Add /auth/request-verification and /auth/verify-email
    Required before treating accounts as fully active.

MEDIUM PRIORITY

[ ] TODO-7: Connector Audit Logging
    Every connector action (discovery, test, write, failure) must be logged
    with: connector_id, user_id, operation, status, timestamp, response_code.

[ ] TODO-8: Per-Connector Rate Limiting
    Each connector should have its own rate limit budget separate from
    the user's campaign generation quota.

[ ] TODO-9: Connector Kill Switch
    Admin and user-level ability to immediately disable a connector without
    deleting it. Status should flip to "disabled" and all pending jobs cancelled.

[ ] TODO-10: Bulk CSV — Remove innerHTML XSS Risk
    The bulk table currently uses innerHTML to render product name and description
    from the CSV. A malicious CSV could inject script tags.
    Switch to textContent or DOMPurify sanitization before rendering.

[ ] TODO-11: Connector Telemetry
    Track which custom connectors are most used across customers.
    This data should drive decisions about which platforms to promote to
    native integrations in server/integrations.py.

LOWER PRIORITY / FUTURE

[ ] TODO-12: Promote Custom Connectors to Native Integrations
    After a custom connector has been widely used and validated, it can be
    promoted to a native push function in server/integrations.py.
    This process should require manual review, not be automated.

[ ] TODO-13: Automated Connector Security Tests (Phase 6)
    Test suite for:
    SSRF, localhost/private IP, redirects, DNS rebinding, invalid OpenAPI,
    oversized specs, invalid methods, unsafe paths, credential leakage,
    malformed responses, timeout behavior, duplicate writes, webhook replay,
    concurrent operations.

[ ] TODO-14: Bulk CSV — Shopify Direct Import
    Instead of CSV upload, allow the customer to authorize Shopify read access
    and KopyKat will fetch their entire product catalog directly from the API.

[ ] TODO-15: Strategy-First Copy Engine Rewrite
    Rewrite the AI generation prompt in ai_engine.py to lead with
    market research and customer pain points before writing copy.
    Currently the prompt is product-first, not strategy-first.

============================================================
ARCHITECTURE REFERENCE
============================================================

server/
  main.py           — FastAPI app, all routes, startup/shutdown
  database.py       — SQLAlchemy models (User, Campaign, UserIntegration, PushJob, etc.)
  models.py         — Pydantic request/response schemas
  auth.py           — JWT, password hashing, credential encryption/decryption
  ai_engine.py      — OpenAI/Gemini generation, analyze_csv_mapping()
  billing.py        — Stripe webhook handler, PLANS dict
  integrations.py   — Push functions for all native platforms + background_push()
  connector_engine.py    — SSRF-safe OpenAPI/Swagger discovery (DO NOT REWRITE)
  connector_registry.py  — Restricted validated connector execution runtime (DO NOT REWRITE)

frontend/
  index.html        — Public landing page (hero, features, pricing, signup/login)
  dashboard.html    — Authenticated customer dashboard (all tabs/sections)

key ENV VARS (must be set in Render):
  DATABASE_URL          — PostgreSQL connection string (production)
  SECRET_KEY            — JWT signing secret
  ENCRYPTION_KEY        — Fernet key for credential encryption
  OPENAI_API_KEY        — GPT-4o (primary AI provider)
  GEMINI_API_KEY        — Gemini (fallback AI provider)
  STRIPE_SECRET_KEY     — Stripe secret
  STRIPE_WEBHOOK_SECRET — Stripe webhook signature verification
  APP_BASE_URL          — https://kopykat.onrender.com

Native platforms currently supported (server/integrations.py):
  wordpress, mailchimp, hubspot, shopify, webflow,
  amazon, ebay, walmart, temu

============================================================
CRITICAL WARNINGS FOR NEXT SESSION
============================================================
1. DO NOT rewrite connector_engine.py or connector_registry.py.
   They are already correct and hardened. Build the UI on top of them.

2. DO NOT wholesale-replace server/main.py. It has happened before and
   caused a major recovery incident. Always make surgical changes.

3. The Bulk CSV table uses innerHTML. TODO-10 must be addressed before
   this feature is considered production-safe.

4. Amazon, eBay, Walmart, and Temu push functions are scaffolded but use
   mock tokens in comments. They need live API credential testing before
   being presented to customers as production-ready.

5. APP_VERSION in frontend HTML controls cache clearing. Current value: 1.0.8
   Increment to 1.0.9 on the next deploy that changes frontend files.

============================================================
HANDOFF POINT
============================================================
The codebase is stable and deployed on Render.
All code is committed and pushed to: https://github.com/thepros2014/kopykat

Start with TODO-1: "Add Any Platform" UI.
Read server/connector_engine.py and server/connector_registry.py first.
Do not rebuild them. Wire them to a new customer-facing UI flow.

END OF HANDOFF
